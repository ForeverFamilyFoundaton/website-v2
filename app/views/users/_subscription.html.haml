.card.personal-information
  .card-header
    Subscription
  .card-body
    %p= t "users.show.subscribed.#{true}"
    = link_to t('users.show.upgrade'), new_subscription_path, class: 'btn btn-warning'
