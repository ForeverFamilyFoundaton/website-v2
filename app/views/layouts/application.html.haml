!!!
%html{lang: 'en'}
  %head
    %meta{charset: 'utf-8'}
    %meta{content: 'IE=edge', 'http-equiv' => 'X-UA-Compatible'}
    %meta{content: 'width=device-width, initial-scale=1', name: 'viewport'}
    / The above 3 meta tags *must* come first in the head; any other head content must come *after* these tags
    %meta{content: '', name: 'description'}
    %meta{content: '', name: 'author'}
    %meta{ content: Rails.application.credentials.stripe[:public_key], name: 'stripe-public-key' }

    = render 'layouts/icons'

    %title Forever Family Foundation

    = stylesheet_link_tag 'https://fonts.googleapis.com/css2?family=Josefin+Sans:wght@200;400;700&display=swap', media: 'all'
    = javascript_include_tag 'https://js.stripe.com/v3'
    = javascript_pack_tag 'application'
    = stylesheet_link_tag 'application', media: 'all'
    = csrf_meta_tags

  %body.main
    %button#sticky-donate-button.btn.btn-warning
      Donate Now!
    %nav.top-nav.fixed-top.navbar.navbar-expand-lg.navbar-dark
      = render 'layouts/top_nav'
    .container-fluid
      = render 'layouts/messages'
      = yield
    = render 'layouts/footer'
